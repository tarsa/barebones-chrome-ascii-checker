{
  "manifest_version": 3,
  "name": "Printable ASCII Checker (suspicious characters finder)",
  "version": "1.0",
  "description": "Check whether text contains only tabs, newlines, spaces, and printable ASCII characters.",
  "action": {
    "default_title": "Printable ASCII Checker",
    "default_popup": "popup.html"
  }
}
